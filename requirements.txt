# Core dependencies
shapely>=2.0.0

# Optional features
Pillow>=10.0.0  # For image handling
watchdog>=3.0.0  # For file watching
pywin32>=306; sys_platform == 'win32'  # Windows auto-reload
# Linux users: install xdotool via your package manager
